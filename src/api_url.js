const server_url = 'http://localhost:3000'
// you should change server url with the address you have pushed ruby on rails app.

export default `${server_url}/api/`

// tctype, impact and domains can be edited or added from server_url
// the server url above is http://localhost:3000, then
// http://localhost:3000/domains/
// http://localhost:3000/impacts/
// http://localhost:3000/tctypes/
// are the addresses to edit them.
